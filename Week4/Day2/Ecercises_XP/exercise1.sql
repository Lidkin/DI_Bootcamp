-- Exercise 1

-- select items from items order by price;

-- select * from items order by price desc;

-- select items from items where price >= 80 order by price desc;

-- select customer_name, last_name from customers where id < 4 order by customer_name;

-- select last_name as name from customers order by last_name desc;